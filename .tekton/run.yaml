---
apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: scratchpad-run-test
spec:
  pipelineRef:
    name: scratchpad-pr
  params:
    - name: repo_url
      value: https://github.com/chmouel/scratchpad/
    - name: revision
      value: simple
  workspaces:
  - name: source
    volumeClaimTemplate:
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
